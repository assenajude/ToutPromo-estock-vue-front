export default class Produit {
    constructor(designProd,unite, descripProd, categorieId) {
            this.designProd = designProd;
            this.descripProd = descripProd;
            this.categorieId = categorieId;
            this.unite = unite
    }

}