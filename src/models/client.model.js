export default class Client {
    constructor(nomClient, contactClient, adresseclient) {
        this.nomClient = nomClient;
        this.contactClient = contactClient;
        this.adresseClient = adresseclient
    }

}